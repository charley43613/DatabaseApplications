========================
CREATING JAVA DATABASE APP
========================

1.MAKE SURE 'database.properties' is in root folder for netbeans project(or change path according to IDE)
2.MAKE SURE all jars are available in the lib folder.
3.Make sure "log4j.properties" is in same package as the rest of source.